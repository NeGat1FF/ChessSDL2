#include "Utils/MoveGenerator.h"

bitboard MoveGenerator::generateAllWhitePawnSinglePushes(bitboard white_pawns, bitboard empty_squares)
{
    return bitboard();
}

bitboard MoveGenerator::generateAllWhitePawnDoublePushes(bitboard white_pawns, bitboard empty_squares)
{
    return bitboard();
}

bitboard MoveGenerator::generateAllWhitePawnAttacks(bitboard white_pawns, bitboard black_pieces)
{
    return bitboard();
}

bitboard MoveGenerator::generateAllWhitePawnEnPassantAttacks(bitboard white_pawns, int enPassantSquare)
{
    return bitboard();
}

bitboard MoveGenerator::generateAllWhitePawnMoves(bitboard white_pawns, bitboard empty_squares, bitboard black_pieces, int enPassantSquare)
{
    return bitboard();
}

bitboard MoveGenerator::generateAllBlackPawnSinglePushes(bitboard black_pawns, bitboard empty_squares)
{
    return bitboard();
}

bitboard MoveGenerator::generateAllBlackPawnDoublePushes(bitboard black_pawns, bitboard empty_squares)
{
    return bitboard();
}

bitboard MoveGenerator::generateAllBlackPawnAttacks(bitboard black_pawns, bitboard white_pieces)
{
    return bitboard();
}

bitboard MoveGenerator::generateAllBlackPawnEnPassantAttacks(bitboard black_pawns, int enPassantSquare)
{
    return bitboard();
}

bitboard MoveGenerator::generateAllBlackPawnMoves(bitboard black_pawns, bitboard empty_squares, bitboard white_pieces, int enPassantSquare)
{
    return bitboard();
}

bitboard MoveGenerator::generateAllKnightMoves(bitboard knights, bitboard empty_squares, bitboard own_pieces)
{
    return bitboard();
}

bitboard MoveGenerator::generateAllBishopMoves(bitboard bishops, bitboard empty_squares, bitboard own_pieces)
{
    return bitboard();
}

bitboard MoveGenerator::generateAllRookMoves(bitboard rooks, bitboard empty_squares, bitboard own_pieces)
{
    return bitboard();
}

bitboard MoveGenerator::generateAllQueenMoves(bitboard queens, bitboard empty_squares, bitboard own_pieces)
{
    return bitboard();
}

bitboard MoveGenerator::generateAllKingMoves(bitboard king, bitboard empty_squares, bitboard own_pieces)
{
    return bitboard();
}
